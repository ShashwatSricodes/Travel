# Production environment variables for Render deployment
NODE_ENV=production
PORT=5000

# MongoDB Atlas Connection - Set this in Render dashboard
# MONGODB_URI will be set automatically by Render from the database service

# CORS origins for production
CORS_ORIGINS=https://evora-travel.netlify.app,https://your-frontend-domain.com